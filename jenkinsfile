node {
    stage('Example') {
    
        echo "Hello jk"
        try {
            sh 'main.py'
        }
        catch (exc) {
            echo 'Something failed, in catch command!'
            throw
        }
    }
}
