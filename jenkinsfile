node {
    stage('Example') {
    
        echo "Hello jk"
        try {
            sh 'python main.py'
        }
        catch (exc) {
            echo 'Something failed, in catch command!'
            
        }
    }
}
